//* Importaciones
const deploys = require('./deploys');
const formatters = require('./formatters');
const { repositoryRegex } = require('./regex');

module.exports = {
    deploys,
    formatters,
    repositoryRegex,
}